## Run locally
1. Set environment variables in `docker-compose.yml`. 
**NOTE**: For know user ID e GUID run the `id` command
2. execute commands
```shell
  cp app/config-dev.php app/config/config.php
  touch app/config/CAN_INSTALL
```
3. Go to http://localhost:8080 and complete installation
4. Enable applications
